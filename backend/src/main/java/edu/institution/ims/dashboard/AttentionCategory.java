package edu.institution.ims.dashboard;

/** Deterministic workflow cohorts used by the monitoring dashboard and directory. */
public enum AttentionCategory {
    PROFILE_INCOMPLETE,
    INTERNSHIP_NOT_SECURED,
    DOCUMENTS_PENDING,
    DIARY_PENDING,
    INTERNSHIP_DETAILS_PENDING
}
